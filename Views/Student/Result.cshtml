@{
    ViewData["UseCustomLayout"] = true;
}
@model McqTask.Models.ResultRecord

<div class="container mt-5">
    <h2 class="mb-4">Exam Result</h2>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Name: @Model.Student.FullName</h5>
            <h5 class="card-title">Name: @Model.Exam.Name</h5>
            <p class="card-text"><strong>Score:</strong> @Model.Score</p>
        </div>
    </div>
    <a class="btn btn-primary mt-4" href="/">Back to Home</a>
    </di
